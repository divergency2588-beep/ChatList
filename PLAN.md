# План реализации ChatList

Документ описывает поэтапный порядок работ по спецификации [PROJECT.md](PROJECT.md).

Статус: этапы 0–7 выполнены.

## Этап 0. Подготовка проекта

1. [x] Зафиксировать назначение: один промт → несколько нейросетей → сравнение ответов → сохранение выбранных строк в SQLite.
2. [x] Выбрать стек: Python 3.11+, SQLite, `requests`, GUI на PyQt6.
3. [x] Создать каркас репозитория: `requirements.txt`, `.env.example`, `.gitignore` (исключить `.env` и `chatlist.db`).
4. [x] Разделить код по требованиям спецификации:
   - `db.py` — доступ к SQLite;
   - `models.py` — логика нейросетей;
   - `network.py` — HTTP-запросы к API;
   - `main.py` — интерфейс.

## Этап 1. База данных

1. [x] Спроектировать схему таблиц `prompts`, `models`, `results`, `settings` (см. [DATABASE.md](DATABASE.md)).
2. [x] Реализовать создание схемы при старте (`CREATE TABLE IF NOT EXISTS`).
3. [x] Добавить первичные ключи `id` и служебные даты `created_at`.
4. [x] Для `models.api_id` хранить **имя переменной окружения**, а не сам API-ключ.
5. [x] Ключи вынести в файл `.env` (`OPENAI_API_KEY`, `DEEPSEEK_API_KEY`, `GROQ_API_KEY` и т.д.).
6. [x] Заполнить стартовые записи: неактивные модели OpenAI / DeepSeek / Groq и настройки `request_timeout`, `temperature`.

## Этап 2. Модуль моделей (`models.py`)

1. [x] Описать сущность `Model`: `id`, `name`, `api_url`, `api_id`, `is_active`.
2. [x] Реализовать сервис поверх `db.py`: список всех моделей, только активные (`is_active = 1`), добавление, изменение, удаление.
3. [x] Не читать секреты из БД: ключ берётся из `os.getenv(api_id)` в сетевом слое.

## Этап 3. Сетевой слой (`network.py`)

1. [x] Реализовать отправку промта в OpenAI-совместимый Chat Completions (`POST` на `api_url`).
2. [x] В запрос передавать `model = name`, ключ — из переменной `api_id`.
3. [x] Обрабатывать отсутствие ключа, HTTP-ошибки и неверный JSON.
4. [x] Опрашивать активные модели параллельно, чтобы GUI не ждал их по очереди.
5. [x] Возвращать для каждой модели пару «текст ответа / ошибка».

## Этап 4. Рабочий процесс в GUI (`main.py`)

### 4.1. Ввод промта

1. [x] Поле ввода нового промта и тегов.
2. [x] Вкладка сохранённых промтов: выбор строки из `prompts` и подстановка в форму.

### 4.2. Отправка запроса

1. [x] По кнопке «Отправить» сохранить промт в `prompts`.
2. [x] Полностью очистить временную таблицу в памяти.
3. [x] Взять модели через `models.where(is_active = 1)`.
4. [x] Отправить промт в каждую модель в фоновом потоке (чтобы окно не зависало).
5. [x] Собрать ответы во **временную таблицу в памяти** (не в SQLite).

### 4.3. Интерфейс результатов

Каждая строка временной таблицы:

- [x] название модели;
- [x] текст ответа;
- [x] чекбокс `selected`.

### 4.4. Сохранение

1. [x] По кнопке «Сохранить выбранные» записать в `results` только строки с `selected = True`.
2. [x] Очистить временную таблицу.

### 4.5. Новый запрос

[x] При новой отправке временная таблица удаляется целиком и создаётся заново после ответов.

## Этап 5. Экраны приложения

1. [x] **Запрос** — ввод, отправка, временные результаты, сохранение, экспорт.
2. [x] **Промты** — история `prompts`, подстановка, удаление.
3. [x] **Модели** — CRUD нейросетей, флаг активности.
4. [x] **Результаты** — постоянные записи `results`.
5. [x] **Настройки** — значения из `settings`, подсказка про `.env`.

## Этап 6. Дополнительные функции

1. [x] Поиск и сортировка во всех таблицах интерфейса.
2. [x] Экспорт выбранных (или всех) результатов в Markdown и JSON.
3. [x] Поддержка OpenAI, DeepSeek, Groq и OpenRouter через один OpenAI-совместимый клиент (различаются `api_url`, `name`, `api_id`).
4. [x] Журнал запросов: таблица `logs` и вкладка «Журнал».

## Этап 7. Проверка и поставка

1. [x] Проверить сценарий: новый промт → несколько ответов → отметка строк → сохранение в `results`.
2. [x] Проверить сценарий: выбор старого промта → повторная отправка → очистка временной таблицы.
3. [x] Убедиться, что ключи не попадают в SQLite.
4. [x] Собрать исполняемый файл:

```powershell
python -m PyInstaller --noconfirm --onefile --windowed --name ChatList main.py
```
